name: CI

# Enforce the same gates that pass locally on every work -> base checkpoint.
on:
  pull_request:
    branches: [main]
  push:
    branches: [dev]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx astro check

      - name: Unit & integration tests
        run: npx vitest run

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: End-to-end tests
        run: npx playwright test

      - name: External book build gate
        run: node scripts/check-external-build.mjs

      - name: Multi-book build gate
        run: node scripts/check-multibook.mjs

      - name: Search build gate
        run: node scripts/check-search.mjs

      - name: Upload Playwright report
        uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
